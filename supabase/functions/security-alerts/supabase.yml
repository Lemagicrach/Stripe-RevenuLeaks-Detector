# Supabase Edge Function Configuration
# This file is optional but helps with local development

version: 1

functions:
  security-alerts:
    verify_jwt: false # Allow cron to trigger without JWT
    import_map: ./import_map.json
